# GenLayer Bradbury testnet deployer key — TESTNET ONLY.
# Copy this file to `contracts/.env` and paste your private key below.
# The real `.env` is gitignored and must NEVER be committed.
PRIVATE_KEY=
