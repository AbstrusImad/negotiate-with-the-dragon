# Copy this file to `frontend/.env`.
# These values are public (the on-chain contract address + network name).
# On Vercel: set them as Environment Variables, with Root Directory = frontend.
VITE_CONTRACT_ADDRESS=0xCA98d7142c8bB468D9891a773602106C11692526

# GenLayer network: bradbury | studionet | localnet
VITE_GENLAYER_NETWORK=bradbury
